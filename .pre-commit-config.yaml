repos:
  - repo: local
    hooks:
      - id: commitlint
        name: commitlint
        language: system
        entry: bash -c 'cd documentation-ui && npx --no -- commitlint --edit "$1"' --
        stages: [commit-msg]
        pass_filenames: true
